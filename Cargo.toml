[dependencies]
leptos = { version = "0.6", features = ["csr"] }
wasm-bindgen = "0.2"

# ВАЖНО: LocalStorage пишется слитно, без подчеркивания!
[dependencies.web-sys]
version = "0.3"
features = [
  "Storage",
  "Window",
  "LocalStorage" 
]
